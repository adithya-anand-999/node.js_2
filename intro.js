//you can run js files using node, syntax: node (name of file with its suffix), so node. example.js
console.log("Hello World");


//the global variables in node can be accessed anywhere in your code. 